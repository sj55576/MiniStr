# AGENTS.md — MiniStr

## プロジェクト概要

- 目的: ブラウザで遊べるターン制戦略ゲーム。地形・視界・補給・生産・占領を含むゲームプレイを提供する。
- 対象環境: モダンブラウザ。GitHub Pages で公開する静的サイト。

## 使用技術

- 言語・バージョン: TypeScript 5.8、strict / noUncheckedIndexedAccess
- フレームワーク・主要ライブラリ: Vite 6、Vitest 3
- 実行環境: Node.js 22 以上、npm 10 以上

## パッケージマネージャー

- TypeScript/JavaScript: npm、ロックファイル: `package-lock.json`
- 依存更新のルール: 要件に必要な場合だけ追加・更新し、ロックファイルを同期する。既存の標準機能で満たせる場合は依存を増やさない。

## ディレクトリ構成

| パス | 役割 | 変更時の注意 |
| --- | --- | --- |
| `src/game/` | UIから独立したゲーム状態・コマンド・戦闘・マップ・セーブ/リプレイ | UI依存を持ち込まず、状態遷移と入力検証をテストする |
| `src/ai/` | 難易度別CPUの判断 | 視認済み情報だけを使う既存制約を守る |
| `src/ui/` | 盤面操作、ズーム、表示補助、演出・音 | タッチ・キーボード操作の既存導線を壊さない |
| `src/main.ts` | ブラウザUIとゲームロジックの統合 | UIの変更とゲーム規則の変更を不要に混在させない |
| `tests/` | Vitestの単体・統合テスト | ゲーム規則を変更する場合は成功・失敗ケースを追加する |
| `public/assets/` | 公開する静的素材 | 権利・容量・参照パスを確認し、不要な置換をしない |
| `scripts/` | 開発・CI補助スクリプト | private-sessionリンク検査を弱めない |
| `.github/` | CI、Pages、Issue/PRテンプレート | fork PR・最小権限・Secrets非利用の方針を維持する |

## アーキテクチャ

- エントリーポイント: `src/main.ts`
- レイヤー・責務: `src/game/` がゲーム規則と状態、`src/ai/` がCPU判断、`src/ui/` がUI補助を担当し、`src/main.ts` が統合する。
- 状態管理: ゲーム状態は不変更新を基本とし、セーブ・リプレイは検証済みのJSON互換形式で扱う。
- 重要な設計制約: UIはゲームロジックへ直接依存しすぎず、ゲーム規則はテスト可能なモジュールに保つ。

## 検証コマンド

| 目的 | コマンド | 実行条件・補足 |
| --- | --- | --- |
| 依存導入 | `npm ci` | lockfileに従って導入する |
| private-sessionリンク検査 | `npm run check:links` | 変更に含めるコミット範囲も確認する |
| 型チェック（テストを含む） | `npm run typecheck:test` | 実装変更時は必須 |
| unit test | `npm test` | 実装変更時は必須 |
| build | `npm run build` | 公開用成果物の確認 |

## 変更禁止領域

- GitHub Pages の公開設定・デプロイ条件、CIの権限、fork PRのSecret利用方針は、明示的な要件なしに変更しない。
- `scripts/check-private-links.sh` と、その検査を回避する設定・テスト緩和を変更しない。
- 公開素材、ライセンス表記、既存のリリース判断記録は、対象タスクと根拠なしに変更しない。
- 不要な依存更新、生成物の直接編集、テスト削除・skip追加での回避をしない。

## DB・API 固有ルール

- DB・サーバーAPIはない。ブラウザの `localStorage` に保存するセーブ、リプレイ、キャンペーン進行、音量設定は外部入力として検証する。
- 保存形式やゲームコマンドを変更する場合は、既存データを安全に拒否または互換的に読み込めることを確認し、関連テストを追加する。
- 外部JSONのインポートでは、サイズ・構造・値域の検証を維持する。

## デプロイ上の注意

- GitHub Pages は `main` へのpush後、型チェック・テスト・ビルドが成功した場合に公開される。
- `main` へ直接反映しない。変更は作業ブランチとPull Requestで提案する。
- Secretや個人情報、private coding sessionへのリンクをコミット・Issue・PR・ログに含めない。

## プロジェクト固有の完了条件

- 変更がゲーム規則、UI操作、セーブ互換性、公開手順のどれに影響するかを明記する。
- 関連するVitestテストを追加または更新し、型チェック・テスト・ビルド・リンク検査を実行する。
- UI変更時は、必要に応じてPCとスマートフォンの主要導線を確認し、PRにスクリーンショットまたは未確認理由を記載する。

<!-- AI-PLATFORM:START -->
## AI Platform 共通ルール（同期管理）

- 変更前に関連実装・設定・テストを確認し、既存の設計と命名を尊重する。
- 必要最小限の差分を選び、外部入力を検証する。TypeScript は型安全性、Python は型ヒントと明確な例外処理を優先する。
- 不具合修正には回帰テストを追加し、lint・型チェック・テスト・ビルドを実行する。テスト削除やチェック無効化で問題を回避しない。
- Secret・個人情報を出力しない。認証、認可、DB、公開 API は根拠なく変更しない。
- 実行していない検証を成功と報告せず、PR には変更内容、テスト結果、リスク・未検証事項を記載する。

詳細: `sj55576/ai-platform` の `prompts/coding-agent-typescript-python.md`
<!-- AI-PLATFORM:END -->
