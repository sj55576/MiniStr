# MiniStr

ブラウザで遊べるターン制戦略ゲームの MVP です。Issue #1 の Phase 1 として、正方形グリッド上での移動・戦闘・反撃・ターン交代・全滅勝利を実装しています。

## 開発

```bash
npm install
npm run dev
```

味方ユニットをクリックしてから、黄色いマスをクリックすると移動します。移動の前後に敵ユニットをクリックすると攻撃できます。各ユニットは各ターンに一度移動、一度攻撃できます。

```bash
npm test
npm run build
```
